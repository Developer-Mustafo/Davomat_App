<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.DavomatApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/dark_yellow</item>
    </style>
    <style name="Theme.EditText" parent="ThemeOverlay.Material3.TextInputEditText">
        <item name="passwordToggleTint">@color/dark_yellow</item>
        <item name="android:textColor">@color/dark_yellow</item>

    </style>
</resources>